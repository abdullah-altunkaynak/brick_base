name: brick_base_example
description: "Example application demonstrating Brick Base package usage."
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.11.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  brick_base:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
